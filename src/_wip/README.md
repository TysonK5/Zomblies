# WIP game systems (not compiled)

These modules are **quarantined** — excluded from `tsconfig` and not mounted in `Game.tsx`.

| File | Purpose |
|------|---------|
| `waveConfig.ts` / `waveSpawner.tsx` | Round-based zombie spawns |
| `weaponPickups.tsx` | World weapon pickups |
| `buyStations.tsx` | Points shop tables |
| `playerHealth.tsx` | Player HP / armor / death |

Bring back by moving into `src/game/` and wiring into `Game.tsx`.
